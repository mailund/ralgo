tree <- empty_splay_tree()

(x <- sample(1:10))
insert(tree, x[1])
plot(tree)

insert(tree, x[2])
plot(tree)

insert(tree, x[3])
plot(tree)

insert(tree, x[4])
plot(tree)

insert(tree, x[5])
plot(tree)

insert(tree, x[6])
plot(tree)

insert(tree, x[6])
plot(tree)
